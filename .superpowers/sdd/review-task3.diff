# Review package: 49a744e..HEAD

## Commits
db70852 feat: add parts API service (getParts, getPartOptions)

## Files changed
 src/services/parts.js | 8 ++++++++
 1 file changed, 8 insertions(+)

## Diff
diff --git a/src/services/parts.js b/src/services/parts.js
new file mode 100644
index 0000000..7fd213c
--- /dev/null
+++ b/src/services/parts.js
@@ -0,0 +1,8 @@
+import { api } from '@/lib/api';
+
+export const getParts = () => api.get('v1/parts').json();
+
+export const getPartOptions = (partId, styleId) => {
+  const searchParams = styleId != null ? { styleId } : {};
+  return api.get(`v1/parts/${partId}/options`, { searchParams }).json();
+};
