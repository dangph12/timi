# Review package: a361f1121e36d2b4a89bc31eb91d095ee55a49cf..HEAD

## Commits
891312b fix: handle array responses in api parseJson

## Files changed
 src/lib/api.js | 5 +++++
 1 file changed, 5 insertions(+)

## Diff
diff --git a/src/lib/api.js b/src/lib/api.js
index 1fd9838..4a2b5ea 100644
--- a/src/lib/api.js
+++ b/src/lib/api.js
@@ -1,10 +1,15 @@
 import ky from 'ky';
 
 export const api = ky.create({
   prefix: import.meta.env.VITE_API_BASE_URL || 'http://localhost:8080',
   headers: { 'Content-Type': 'application/json' },
   parseJson: (text) => {
     const { status, message, data } = JSON.parse(text);
+    if (Array.isArray(data)) {
+      data.status = status;
+      data.message = message;
+      return data;
+    }
     return { ...data, status, message };
   }
 });
